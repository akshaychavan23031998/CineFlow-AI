# CineFlow AI Local Infrastructure

CineFlow AI uses Docker Compose to provide reproducible local infrastructure dependencies for development.

The authoritative local Compose definition is located at:

```text
compose.yaml